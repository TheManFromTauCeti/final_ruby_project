<h2>Congradulations! Here's what you ordered:</h2>
<ul>
<% sum = 0 %>
<% gst = @order.GST%>
<% hst = @order.HST %>
<% pst = @order.PST %>
<% @processed_items.each do |item| %>
  <li>
    <div class="row">
      <div class="col-md">
        <%= image_tag item.product.image.variant(resize_to_limit: [100, 100]) %>
      </div>
      <div class="col-md">
        <%= item.product.name %>
        price: <%= item.item_price %>
        quantity: <%= item.quantity %>
      </div>
    </div>
  </li>
  <hr />
  <% sum += item.item_price.to_d %>
<% end %>
<h2>GST: <%= gst %></h2>
<h2>HST: <%= hst %></h2>
<h2>PST: <%= pst %></h2>
<h1>Total: $<%= sum * (hst.to_d + gst.to_d + pst.to_d + 1) %></h1>
<h4>You did it Adam, I'm proud of you!</h4>